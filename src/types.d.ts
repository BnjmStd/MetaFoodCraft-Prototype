export interface classMode {
    className: string
}

export interface Food {
    id: number
    nombre: string
    marca: string
    precio: float
    cantidad: number
    categoria: string
    imagen: string
}